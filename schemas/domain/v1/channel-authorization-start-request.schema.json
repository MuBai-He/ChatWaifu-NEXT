{
  "$defs": {
    "ChannelAuthorizationMethod": {
      "description": "Interactive authorization methods exposed to trusted local clients.",
      "enum": [
        "qr_code"
      ],
      "title": "ChannelAuthorizationMethod",
      "type": "string"
    }
  },
  "$id": "https://chatwaifu.local/schemas/domain/v1/channel-authorization-start-request.schema.json",
  "description": "Start one local, short-lived provider authorization session.\n\nProvider credentials and account identities must never be accepted from the\nbrowser. A successful adapter authorization derives them and returns only a\nsanitized ``ChannelConnectionSnapshot``.",
  "properties": {
    "character_id": {
      "maxLength": 256,
      "minLength": 1,
      "title": "Character Id",
      "type": "string"
    },
    "connection_name": {
      "anyOf": [
        {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Connection Name"
    },
    "method": {
      "$ref": "#/$defs/ChannelAuthorizationMethod",
      "default": "qr_code"
    },
    "principal_scope": {
      "default": "local",
      "description": "Runtime-owned relationship and memory scope, never a provider identity",
      "maxLength": 256,
      "minLength": 1,
      "title": "Principal Scope",
      "type": "string"
    },
    "provider_id": {
      "pattern": "^[a-z0-9][a-z0-9._-]{1,127}$",
      "title": "Provider Id",
      "type": "string"
    },
    "schema_version": {
      "const": "1.0",
      "default": "1.0",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "provider_id",
    "character_id"
  ],
  "title": "ChannelAuthorizationStartRequest",
  "type": "object",
  "x-schema-version": "1.0"
}
